@orders.map do |order|
  {
    id: @order.id,
    user_id: @order.user_id,
    product_id: @order.product_id,
    price: @order.price,
    quantity: order.quantity,
    sub_total: @order.price,
    tax: @order.tax,
    total: @order.total,
  }
end
